apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: front-end-virtualservice
  namespace: sockshop
spec:
  hosts:
  - "front-end"
  http:
  - route:
    - destination:
        host: $FRONT_END_POD
        port:
          number: 8079
